# Google OAuth Client IDs
# Get these from Google Cloud Console → APIs & Services → Credentials
# The app works without these; Google Sign-In will be disabled.
GOOGLE_CLIENT_ID_WEB=
GOOGLE_CLIENT_ID_ANDROID=
